version: '3.8'

services:
  postgres:
    image: postgres:18
    container_name: postgres-db
    environment:
      - POSTGRES_DB=imperiumRooms
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=root
    ports:
      - "5432:5432"
    restart: always
    volumes:
      - postgres-data:/var/lib/postgresql
    networks:
      - app-network

  app:
    image: imperiumroomsds3:1.0
    container_name: imperiumroomsds3
    depends_on:
      - postgres
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/imperiumRooms
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=root
    networks:
      - app-network

volumes:
  postgres-data:

networks:
  app-network:

